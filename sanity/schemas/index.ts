import { postSchema } from './post';
import { eventSchema } from './event';
import { resourceSchema } from './resource';
import { jobSchema } from './job';

export const schemaTypes = [postSchema, eventSchema, resourceSchema, jobSchema];

